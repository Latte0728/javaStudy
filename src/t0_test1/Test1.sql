show tables;

idx  int  not null auto_increment primary key,
  name varchar(20) not null;
  
  desc test1;
  
  select * from test1;
  
  insert into set values(default, mid, '아이디');
  insert into set values(default, gender, '성별');
  insert into set values(default, ipsail, '입사일');
  
  
  